import styled from "styled-components";
import { Typography, Input as Inp } from "antd";

const { Text } = Typography;

export const Input = styled(Inp)``;

export const Label = styled(Text)`
  font-weight: 600;
  overflow: hidden;
  color: #132d4c !important;
  margin-bottom: 5px;
`;
